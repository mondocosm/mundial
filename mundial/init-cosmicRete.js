// init-cosmicRete.js - Temporary script to initialize CosmicRete (Rete.js fork) in isolation

import { initializeCosmicReteEditor } from './cosmicRete-editor.js'; // Updated filename and function name
import { makeDraggable } from './ui.js'; // Need this for the panel

document.addEventListener('DOMContentLoaded', () => {
    // console.log("DOM Content Loaded - Rete initialization moved to main.js");
    // Initialization logic removed to prevent double initialization.
    // Draggable logic also moved to main.js.
});