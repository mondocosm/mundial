# Todo List for Mundial514 Development

## High Priority
- [ ] Fix globe window not displaying.
- [ ] Correct top button functionality:
    - [ ] Ensure buttons are simple on/off toggles for their respective panels.
    - [ ] Buttons should be highlighted (e.g., with an 'active' class) when their panel is visible.
    - [ ] Ensure button logic does not interfere with globe rendering.
    - [ ] Make sure `globe-panel` is visible by default and `globe-view-btn` is active.
    - [ ] Make sure `map-panel` is hidden by default and `map-view-btn` is inactive.

## Medium Priority
- [ ] Review and refine `initializeOpenGlobus()` to ensure stability.
- [ ] Review and refine `initializeOpenLayersMap()` if map view issues persist.
- [ ] Investigate any console errors after the globe and button fixes.

## Low Priority
- [ ] Add items from previous todo list if recovered or remembered.
- [ ] UI/UX enhancements for panels (e.g., consistent styling, positioning).

## Backlog / Future
- [ ] Implement features from `CONCEPT_DESIGN_OUTLINE.md` and `MVP_SCOPE.md`.